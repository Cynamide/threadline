# @threadline/skill-templates

Markdown instruction templates copied into repo-local Threadline agent skills.

Most users should run `threadline init` from `@threadline/cli`; init installs the generated skill file in the repo. Use this package directly only when building custom setup tooling that needs the canonical Threadline agent guidance.
