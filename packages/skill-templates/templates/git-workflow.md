# Git Workflow

Check `git status` before you edit.
Do not overwrite unrelated work.
Make focused commits when a task is done.
Use clear commit messages that describe the user-visible change.
If the repo already has a branch or release convention, follow it.
