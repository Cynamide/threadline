# Local-first validation before push

UI Copilot validates handoffs and boundary rules locally before code is pushed. The point is to catch AI-generated mistakes at the edge of the repo, shorten feedback loops, and avoid treating shared CI as the first line of defense for syntax and scope errors.
